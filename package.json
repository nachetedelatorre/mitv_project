{
  "name": "mitv_project_root",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd panel && npm ci --legacy-peer-deps && npm run build --if-present && cd .. && cd backend && npm ci --legacy-peer-deps",
    "start": "cd backend && node server.js",
    "build": "cd panel && npm ci --legacy-peer-deps && npm run build --if-present"
  }
}
